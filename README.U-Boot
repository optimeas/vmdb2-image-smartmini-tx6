Starting with a U-Boot from our Yocto image (in the eMMC), the following variables need to be adjusted:

setenv scraddr 30000000
setenv bootcmd "ext4load mmc 1:1 "$scraddr" /boot/u-boot.scr || ext4load mmc 0:1 $scraddr /boot/u-boot.scr; source $scraddr"
save
